# Local development / unit-testing dependencies for the Databricks migration.
# Databricks clusters run DBR 14.3 LTS (Spark 3.5, Python 3.10); these pins mirror that
# runtime so local tests behave like the cluster.
pyspark==3.5.1
delta-spark==3.1.0

# Notebook parameters / config
PyYAML==6.0.1

# Excel source (data_sources/transaction_excel.xlsx) for local fixtures
openpyxl==3.1.2
pandas==2.2.2

# Test + lint tooling
pytest==8.2.2
pytest-cov==5.0.0
ruff==0.5.5
